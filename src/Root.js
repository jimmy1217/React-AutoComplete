import React from 'react';
import { Autocomplete } from 'components';

const Root = () => {
    return (
        <div className="p-40">
            <Autocomplete />
        </div>
    );
};

export default Root;